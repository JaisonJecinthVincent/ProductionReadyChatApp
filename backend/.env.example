# Development environment variables for the chat application

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/chat-app

# JWT Secret (use a strong secret in production)
JWT_SECRET=your-super-secret-jwt-key-here

# Redis Configuration (optional for development)
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_PASSWORD=your-redis-password

# Cloudinary Configuration (for image uploads)
CLOUDINARY_CLOUD_NAME=your-cloudinary-cloud-name
CLOUDINARY_API_KEY=your-cloudinary-api-key
CLOUDINARY_API_SECRET=your-cloudinary-api-secret

# OAuth Configuration (optional)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Server Configuration
PORT=5001
NODE_ENV=development

# Redis Pool Size (optional)
REDIS_POOL_SIZE=5

# Session Secret
SESSION_SECRET=your-session-secret-key

# Server ID for clustering (auto-generated if not set)
# SERVER_ID=server-1

# Elasticsearch Configuration (for message search)
ELASTICSEARCH_ENABLED=true
ELASTICSEARCH_URL=http://localhost:9200
# ELASTICSEARCH_USERNAME=elastic
# ELASTICSEARCH_PASSWORD=your-elasticsearch-password

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Queue Admin (for Bull queue monitoring)
QUEUE_ADMIN_KEY=your-queue-admin-key
